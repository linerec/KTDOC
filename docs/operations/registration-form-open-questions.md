# 2026–2027 수강 신청서 — 선생님 확인 대기 항목

최초 작성 2026-08-14 · **갱신 2026-08-22** · 대상 폼: `forms.slug = '2026-2027-regular'`
(**접수 중**, schema v8, 응답 2건)

**선생님께 보여드릴 문서**: https://claude.ai/code/artifact/a08d32d9-06e5-4ff9-a593-3ff02f3f3e7a
(비전문가용 웹 페이지. 번호로 답할 수 있게 만들었고 인쇄도 됩니다.)

---

## 이 문서가 있는 이유

두 갈래가 섞여 있다.

1. **게시 전부터 남아 있던 잠정 판단** — 구글폼과 학비표만으로는 확정할 수 없어 근거를 적어
   잠정 결정을 내린 것들. 코드상 단일 소스는 `lib/forms/provisionalNotes.ts`이고,
   `lib/forms/presets.test.ts`가 노트와 실제 선택지의 짝을 잠근다(한쪽만 사라질 수 없다).
2. **2026-08-21 테스트 피드백에서 새로 생긴 결정** — 실제로 신청해 보신 분이 주신 의견 9건 중,
   판단이 필요 없는 버그 4건은 이미 고쳐 배포했고 나머지가 여기 남았다.

## 확정된 것

| | 내용 | 언제 |
|---|---|---|
| ✅ | 기초 난타반을 1 Drum / 3 Drum으로 분리. 학비 $400 / $450 | 2026-08-13 |
| ✅ | "오고무·동고"를 삼고무·동고 / 오고무로 분리. 학비 $600 / $700 | 2026-08-13 |
| ✅ | **형제·자매 10% 할인은 실재한다.** 실폼 `info_tuition`에 "형제·자매가 함께 등록할 경우, 각 학생의 수강료에 10% 할인" 문구가 있다(관리 화면에서 추가된 것이라 `presets.ts`에는 없다). **계산 방법**만 미정 | 2026-08-22 확인 |
| ✅ | 학년 문항을 선택으로. 성인 수강생이 제출하지 못하던 자리 | 2026-08-21 |

### `youth-repertoire-missing` — 사실상 확정, 코드 정리만 남음

실폼의 선택지 라벨이 **"중고등부 한국무용 작품반(3:15-4:00P.M.)"** 으로, 게시 후 수업 시간이
채워져 있다. 없는 수업에 시간을 넣지는 않으므로 판단(고급반 무용과 별개 수업)이 맞았다고 본다.
**다만 명시적 확인은 받지 않았으므로 `provisionalNotes.ts`에서 아직 지우지 않았다.**
선생님께 한마디 받으면 지운다. (게시·잠금 이후라 어차피 되돌릴 수 없다 —
`assertEditAllowed`가 선택지 삭제를 409로 막는다.)

## 확인 대기 — 질문지의 5가지

| # | 질문 | 막고 있는 것 |
|---|---|---|
| 1 | 신청자에게 학비 금액을 보여줄 것인가 (전액 / 안 함 / 시작가) | 제출 전 요약의 금액 줄, 기간 옆 금액 표시 |
| 2 | 학비표에 없는 4수업의 학비 + Sat+Sun 패키지 조건 | 1번을 켜면 **틀린 금액이 노출된다** |
| 3 | 삼고무/오고무·난타 1드럼/3드럼을 신청서에서 합칠 것인가 | 과목 선택지 구조 (이미 잠김 — 합치려면 툼스톤 작업) |
| 4 | 형제·자매 10%의 계산 방법 (가: 전원/둘째부터, 나: 묶음가 뒤 중복, 다: 3개월 가능?) | 고지서 자동 계산 |
| 5 | 등록금 고지서를 자동 생성할 것인가 | 1~4가 선행 |
| 추가 | 삼고무·오고무 정원(`class-capacity`) | 명단의 "7 / 10" 표시 |
| 추가 | 영문 `info_tuition` 문구가 어긋남 — 고칠지 | 영어 화면 가독성 |

### 1번이 뒤집는 설계 결정

지금 설계는 **"신청만 받고 학비는 확인 후 개별 안내"** 이고 `lib/forms/tuition.ts` 머리말에
"신청자에게는 절대 보이지 않는다"고 못박혀 있다. 켜려면 그 문장부터 고쳐야 한다.

기술적 제약 둘:

- 금액은 기간만으로 정해지지 않는다. **조합마다 다른 확정 금액표**다
  (`1 Dance $400 + Kids Drum 1 $400 = $650`). "3개월 $___" 형태로 못 쓴다.
- 지금 폼은 **기간(q6)이 과목(q7)보다 먼저**다. 금액을 기간 옆에 붙이려면 순서를 바꾸거나,
  과목을 고른 뒤 기간 라벨의 금액이 갱신되는 방식이어야 한다.

### 4번 — 인보이스를 "기본가 − 할인"으로 분해할 수 없다

학비표는 산식이 아니라 조합별 확정 금액이다. `$400 + $400`이 `$800`이 아니라 `$650`이고
그 차액이 곧 다과목 할인이다. `기본 $800 − 할인 $150`으로 적으면 **없는 숫자를 지어내는 것**이 된다.
인보이스는 `선택 조합 가격 → 조정 → 최종` 구조여야 한다. 형제 할인 10%는 그 위에 얹는 별개 층이다.

## 남은 정리 작업

- [ ] 질문지 5가지 회수
- [ ] `youth-repertoire-missing` 확인 후 `provisionalNotes.ts`에서 삭제
- [ ] `sunday-adult-tuition` · `kdrum-is-mega-drum` · `class-capacity` 회수 후 삭제
      (앞의 둘은 `courseCode`, 정원은 편집 화면 값 — 배포 불필요)
- [ ] 배열이 비면 `provisionalNotes.ts` 자체와 편집 화면 배너 제거
- [ ] `presets.test.ts`의 노트 짝 검사 시험도 함께 정리
- [ ] 이 문서를 "해결됨"으로 표시하거나 삭제

## 관련

- 설계: `docs/superpowers/specs/2026-08-13-registration-forms-design.md` §7.12
- 원본 자료: `docs/superpowers/specs/2026-08-13-registration-forms-source.md`
- 학비표 룩업: `lib/forms/tuition.ts`
- 제출 전 요약: `lib/forms/summary.ts` (금액 자리를 비워 둔 이유가 주석에 있다)
